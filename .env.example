# Binance Trading System - Environment Configuration
# ==============================================================================
# QUICK START:
# 1. Copy this file to .env: cp .env.example .env
# 2. Edit .env and set TRADING_MODE and your API keys
# 3. Run: npm start (this automatically generates the environment files)
# ==============================================================================

# ==============================================================================
# TRADING MODE SELECTION (MOST IMPORTANT SETTING!)
# ==============================================================================
# Set to 'demo', 'testnet', or 'live'
# - demo: Uses mock data, no real API calls (default, safe for testing UI)
# - testnet: Uses Binance testnet with test funds (recommended for testing)
# - live: Uses real Binance account with real money (⚠️ USE WITH EXTREME CAUTION!)

TRADING_MODE=demo

# Proxy Server Configuration (RECOMMENDED to avoid CORS issues)
# Set to true to use the backend proxy server
USE_PROXY=true
PROXY_URL=http://localhost:3000
PROXY_PORT=3000


# ==============================================================================
# TESTNET API KEYS (RECOMMENDED FOR TESTING)
# ==============================================================================
# Get your testnet API keys from: https://testnet.binance.vision/
# 1. Register a free account on testnet.binance.vision
# 2. Click "Generate HMAC_SHA256 Key"
# 3. Copy your API Key and Secret Key here

BINANCE_TESTNET_API_KEY=
BINANCE_TESTNET_API_SECRET=


# ==============================================================================
# LIVE API KEYS (⚠️ ONLY FOR PRODUCTION!)
# ==============================================================================
# Get your API keys from: https://www.binance.com/en/my/settings/api-management
# ⚠️ WARNING: These control real money! Test thoroughly in testnet first!

BINANCE_API_KEY=
BINANCE_API_SECRET=


# ==============================================================================
# HOW TO USE:
# ==============================================================================
#
# FOR DEMO MODE (Default - No setup needed):
#   TRADING_MODE=demo
#   → Just run: npm start
#   → Shows mock data, no API calls
#
# FOR TESTNET MODE (Recommended for testing):
#   1. Get testnet keys from https://testnet.binance.vision/
#   2. Set: TRADING_MODE=testnet
#   3. Set: USE_PROXY=true (to avoid CORS issues)
#   4. Fill in BINANCE_TESTNET_API_KEY and BINANCE_TESTNET_API_SECRET
#   5. Run in 2 terminals:
#      Terminal 1: npm run proxy
#      Terminal 2: npm start
#   → Shows real testnet data with test funds
#
# FOR LIVE MODE (⚠️ Real money!):
#   1. Get live keys from https://www.binance.com/en/my/settings/api-management
#   2. Set: TRADING_MODE=live
#   3. Fill in BINANCE_API_KEY and BINANCE_API_SECRET
#   4. ⚠️ NEVER enable withdrawal permissions!
#   5. ⚠️ Enable IP whitelist restrictions!
#   6. Run: npm start
#   → Shows real account data and can execute real trades!
#
# ==============================================================================


# ==============================================================================
# SECURITY BEST PRACTICES
# ==============================================================================
#
# API Key Permissions (Recommended settings):
# ✅ Enable Reading
# ✅ Enable Spot & Margin Trading (if needed for trading)
# ❌ NEVER enable Withdrawals (critical security measure!)
# ✅ Enable IP Access Restrictions (whitelist your IP address)
#
# Additional Security:
# - NEVER commit the actual .env file to Git (it's in .gitignore)
# - NEVER share your API keys with anyone
# - ALWAYS test with testnet before going live
# - ALWAYS use 2FA on your Binance account
# - REGULARLY rotate your API keys
# - MONITOR your account activity regularly
#
# ==============================================================================


# ==============================================================================
# IMPORTANT NOTES
# ==============================================================================
#
# 1. The .env file is NOT committed to Git (it's in .gitignore)
# 2. When you run "npm start", it automatically runs "npm run generate-env"
#    which reads this .env file and generates src/environments/environment.ts
# 3. You'll see console output confirming which mode is active:
#    - 🎮 Demo mode activated - using mock data
#    - 🧪 Testnet mode activated - using real Binance Testnet API
#    - 💰 Live mode activated - using real Binance API
# 4. If you change .env, restart the dev server to regenerate environment files
#
# ==============================================================================
